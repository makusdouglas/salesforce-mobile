/**
 * Width in dp at or above which {@link useViewport} classifies the device as a tablet.
 *
 * Matches the catalog feature's constant; a shared `src/app/responsive/` module
 * is pre-approved for when a third consumer arrives (research R-008 of 007).
 */
export const TABLET_MIN_WIDTH = 768;
